{
  "name": "conduit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts-ts start",
    "build": "react-scripts-ts build && react-snapshot",
    "test": "react-scripts-ts test --env=jsdom",
    "eject": "react-scripts-ts eject",
    "postreceive": "npm run build",
    "precommit": "lint-staged && bash -c 'CI=true npm test'",
    "prepush": "lint-staged && bash -c 'CI=true npm run build'",
    "prettier": "bash -c 'prettier ./src/**/*.ts ./src/**/*.tsx -l'",
    "prettier-write": "bash -c 'prettier ./src/**/*.ts ./src/**/*.tsx --write'",
    "tslint": "tslint --project tsconfig.json",
    "tslint-check": "tslint-config-prettier-check ./tslint.json",
    "tslint-fix": "tslint --project tsconfig.json --fix",
    "fix": "npm run prettier-write; npm run tslint-fix",
    "tsc": "tsc --noEmit"
  },
  "lint-staged": {
    "definitions/**/*.ts": [
      "npm run tsc",
      "git add"
    ],
    "src/**/*.{ts,tsx}": [
      "npm run tsc",
      "npm run tslint",
      "npm run prettier",
      "git add"
    ]
  },
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-helmet": "^5.2.0",
    "react-scripts-ts": "2.8.0",
    "react-snapshot": "^1.3.0"
  },
  "devDependencies": {
    "@types/jest": "^21.1.8",
    "@types/node": "^8.0.57",
    "@types/react": "^16.0.28",
    "@types/react-dom": "^16.0.3",
    "@types/react-helmet": "^5.0.3",
    "husky": "^0.14.3",
    "lint-staged": "^6.0.0",
    "prettier": "^1.9.1",
    "tslint": "^5.8.0",
    "tslint-config-prettier": "^1.6.0",
    "typescript": "^2.6.2"
  }
}
